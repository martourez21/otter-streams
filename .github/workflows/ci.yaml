#name: Java CI with Maven
#
#on:
#  push:
#    branches: [ main, develop ]
#  pull_request:
#    branches: [ main ]
#  release:
#    types: [ published ]
#
#jobs:
#  build:
#    runs-on: ubuntu-latest
#    strategy:
#      matrix:
#        java: [ '11', '17' ]
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Set up JDK ${{ matrix.java }}
#        uses: actions/setup-java@v4
#        with:
#          java-version: ${{ matrix.java }}
#          distribution: 'temurin'
#          cache: 'maven'
#
#      - name: Build with Maven
#        run: mvn -B clean compile -DskipTests
#
#      - name: Run Tests
#        run: mvn -B test
#        continue-on-error: true  # Remove this when tests are implemented
#
#  publish:
#    needs: build
#    runs-on: ubuntu-latest
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main' || github.event_name == 'release'
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Set up JDK 11
#        uses: actions/setup-java@v4
#        with:
#          java-version: '11'
#          distribution: 'temurin'
#          cache: 'maven'
#          server-id: github
#          settings-path: ${{ github.workspace }}
#
#      - name: Build and generate Javadoc
#        run: mvn -B clean javadoc:jar source:jar compile -DskipTests
#
#      - name: Publish to GitHub Packages
#        run: mvn -B deploy -DskipTests
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#
#  publish-release:
#    needs: build
#    runs-on: ubuntu-latest
#    if: github.event_name == 'release'
#
#    steps:
#      - uses: actions/checkout@v4
#
#      - name: Set up JDK 11
#        uses: actions/setup-java@v4
#        with:
#          java-version: '11'
#          distribution: 'temurin'
#          cache: 'maven'
#          server-id: github
#          settings-path: ${{ github.workspace }}
#
#      - name: Build release artifacts
#        run: |
#          mvn -B clean javadoc:jar source:jar compile -DskipTests
#          mvn -B package -DskipTests
#
#      - name: Upload Javadoc to GitHub Pages
#        uses: peaceiris/actions-gh-pages@v3
#        with:
#          github_token: ${{ secrets.GITHUB_TOKEN }}
#          publish_dir: ./target/site/apidocs
#          destination_dir: javadoc
#
#      - name: Upload Release Assets
#        uses: softprops/action-gh-release@v1
#        with:
#          files: |
#            **/target/*.jar
#            !**/target/original-*.jar
#            !**/target/*-tests.jar
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}