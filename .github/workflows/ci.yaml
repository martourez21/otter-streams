name: Otter-Streams CI Pipeline with Maven

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  release:
    types: [ published ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java: [ '11', '17' ]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK ${{ matrix.java }}
        uses: actions/setup-java@v4
        with:
          java-version: ${{ matrix.java }}
          distribution: 'temurin'
          cache: 'maven'

      - name: Install all modules to local repository
        run: mvn -B clean install -DskipTests

      - name: Run Tests
        run: mvn -B test
        continue-on-error: true

      - name: Generate Test Report
        if: always()
        run: mvn surefire-report:report-only
        continue-on-error: true

      - name: Upload Test Results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-jdk-${{ matrix.java }}
          path: |
            **/target/surefire-reports/
            **/target/site/

  code-quality:
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install modules first
        run: mvn -B clean install -DskipTests

      - name: Run Maven Verify
        run: mvn -B verify -DskipTests

      - name: Check Code Style (if configured)
        run: |
          if grep -q "maven-checkstyle-plugin" pom.xml; then
            mvn checkstyle:check
          else
            echo "Checkstyle not configured, skipping"
          fi
        continue-on-error: true

  package:
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/develop')

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'

      - name: Install and package artifacts
        run: mvn -B clean install -DskipTests

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: maven-artifacts
          path: |
            **/target/*.jar
            !**/target/original-*.jar
            !**/target/*-tests.jar
          retention-days: 7

  publish:
    needs: [build, code-quality]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    permissions:
      contents: write
      packages: write
      pages: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'

      - name: Configure Maven settings for GitHub Packages
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << EOF
          <settings>
            <servers>
              <server>
                <id>github</id>
                <username>\${env.GITHUB_ACTOR}</username>
                <password>\${env.GITHUB_TOKEN}</password>
              </server>
            </servers>
          </settings>
          EOF

      - name: Set version to SNAPSHOT for main branch
        run: |
          CURRENT_VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          if [[ ! "$CURRENT_VERSION" == *-SNAPSHOT ]]; then
            mvn versions:set -DnewVersion="${CURRENT_VERSION}-SNAPSHOT" -DgenerateBackupPoms=false
          fi

      - name: Build and generate documentation
        run: mvn -B clean install javadoc:jar source:jar -DskipTests -Dmaven.javadoc.failOnError=false -Dmaven.javadoc.failOnWarnings=false

      - name: Verify Javadoc generation
        run: |
          echo "Checking for generated Javadoc JARs..."
          find . -name "*-javadoc.jar" -type f
          echo "Checking for Javadoc directories..."
          find . -path "*/target/apidocs" -type d

      - name: Upload Javadoc artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: javadoc-artifacts
          path: |
            **/target/*-javadoc.jar
            **/target/apidocs/
          retention-days: 7

      - name: Check if version exists in GitHub Packages
        id: check_version
        run: |
          VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)
          if curl -s -H "Authorization: token ${{ secrets.GITHUB_TOKEN }}" \
             "https://maven.pkg.github.com/martourez21/otter-streams/com/codedstreams/otter-streams/$VERSION/otter-streams-$VERSION.pom" \
             | grep -q "404"; then
            echo "exists=false" >> $GITHUB_OUTPUT
          else
            echo "exists=true" >> $GITHUB_OUTPUT
          fi

      - name: Publish to GitHub Packages
        if: steps.check_version.outputs.exists == 'false'
        run: mvn -B deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Skip deployment - version exists
        if: steps.check_version.outputs.exists == 'true'
        run: echo "Version already exists in GitHub Packages, skipping deployment"

      - name: Generate aggregated Javadoc for GitHub Pages
        run: mvn -B javadoc:aggregate -Dmaven.javadoc.failOnError=false -Dmaven.javadoc.failOnWarnings=false
        continue-on-error: true

      - name: Get current version for Javadoc path
        id: get_version
        run: echo "VERSION=$(mvn help:evaluate -Dexpression=project.version -q -DforceStdout)" >> $GITHUB_OUTPUT

      - name: Check if Javadoc was generated
        id: check_javadoc
        run: |
          if [ -d "target/site/apidocs" ]; then
            echo "exists=true" >> $GITHUB_OUTPUT
            echo "✅ Javadoc generated successfully"
            ls -la target/site/apidocs/
          else
            echo "exists=false" >> $GITHUB_OUTPUT
            echo "⚠️ Javadoc directory not found"
          fi

      - name: Deploy Javadoc to GitHub Pages
        if: always() && steps.check_javadoc.outputs.exists == 'true'
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./target/site/apidocs
          destination_dir: javadoc/${{ steps.get_version.outputs.VERSION }}
          keep_files: true
        continue-on-error: true

      - name: Create Javadoc index page
        if: always()
        run: |
          mkdir -p gh-pages-root
          cat > gh-pages-root/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Otter Streams - Production ML Inference for Apache Flink</title>
            <meta name="description" content="Production-grade ML inference library for Apache Flink streaming applications">
            <link rel="icon" href="https://raw.githubusercontent.com/martourez21/otter-streams/main/docs/assets/otterstream-sdk-icon.ico" type="image/x-icon">
            <link rel="preconnect" href="https://fonts.googleapis.com">
            <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
            <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
            <style>
              * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
              }
          
              :root {
                --primary: #667eea;
                --primary-dark: #5568d3;
                --secondary: #764ba2;
                --accent: #4facfe;
                --dark-bg: #0f1419;
                --card-bg: #1a1f2e;
                --text-primary: #e4e6eb;
                --text-secondary: #8b92a8;
                --border: #2d3748;
                --success: #00d9a5;
              }
          
              body {
                font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
                line-height: 1.6;
                color: var(--text-primary);
                background: var(--dark-bg);
                min-height: 100vh;
              }
          
              /* Header */
              .header {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                padding: 0;
                position: relative;
                overflow: hidden;
              }
          
              .header::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                background: url('data:image/svg+xml,<svg width="100" height="100" xmlns="http://www.w3.org/2000/svg"><defs><pattern id="grid" width="50" height="50" patternUnits="userSpaceOnUse"><path d="M 50 0 L 0 0 0 50" fill="none" stroke="rgba(255,255,255,0.05)" stroke-width="1"/></pattern></defs><rect width="100" height="100" fill="url(%23grid)"/></svg>');
                opacity: 0.3;
              }
          
              .nav {
                max-width: 1400px;
                margin: 0 auto;
                padding: 20px 40px;
                display: flex;
                justify-content: space-between;
                align-items: center;
                position: relative;
                z-index: 1;
              }
          
              .logo-section {
                display: flex;
                align-items: center;
                gap: 15px;
              }
          
              .logo {
                width: 45px;
                height: 45px;
                background: white;
                border-radius: 8px;
                padding: 8px;
                display: flex;
                align-items: center;
                justify-content: center;
              }
          
              .logo img {
                width: 100%;
                height: 100%;
                object-fit: contain;
              }
          
              .brand {
                color: white;
                font-size: 1.4em;
                font-weight: 700;
              }
          
              .nav-links {
                display: flex;
                gap: 30px;
                align-items: center;
              }
          
              .nav-link {
                color: rgba(255,255,255,0.9);
                text-decoration: none;
                font-weight: 500;
                font-size: 0.95em;
                transition: color 0.3s;
              }
          
              .nav-link:hover {
                color: white;
              }
          
              /* Hero Section */
              .hero {
                max-width: 1400px;
                margin: 0 auto;
                padding: 100px 40px 120px;
                text-align: center;
                position: relative;
                z-index: 1;
              }
          
              .hero h1 {
                font-size: 3.5em;
                font-weight: 700;
                margin-bottom: 20px;
                color: white;
                line-height: 1.2;
              }
          
              .hero p {
                font-size: 1.3em;
                color: rgba(255,255,255,0.9);
                max-width: 700px;
                margin: 0 auto 40px;
                font-weight: 300;
              }
          
              .version-tag {
                display: inline-block;
                background: rgba(255,255,255,0.2);
                backdrop-filter: blur(10px);
                padding: 8px 20px;
                border-radius: 20px;
                color: white;
                font-weight: 600;
                font-size: 0.9em;
                margin-bottom: 20px;
              }
          
              /* Main Content */
              .container {
                max-width: 1400px;
                margin: -60px auto 0;
                padding: 0 40px 80px;
                position: relative;
                z-index: 2;
              }
          
              /* Cards Grid */
              .cards-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
                gap: 30px;
                margin-bottom: 60px;
              }
          
              .card {
                background: var(--card-bg);
                border-radius: 12px;
                padding: 40px;
                border: 1px solid var(--border);
                transition: transform 0.3s, box-shadow 0.3s, border-color 0.3s;
                position: relative;
                overflow: hidden;
              }
          
              .card::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                right: 0;
                height: 3px;
                background: linear-gradient(90deg, var(--primary), var(--secondary));
                transform: scaleX(0);
                transition: transform 0.3s;
              }
          
              .card:hover {
                transform: translateY(-5px);
                box-shadow: 0 20px 40px rgba(102, 126, 234, 0.15);
                border-color: var(--primary);
              }
          
              .card:hover::before {
                transform: scaleX(1);
              }
          
              .card-icon {
                width: 60px;
                height: 60px;
                background: linear-gradient(135deg, var(--primary), var(--secondary));
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 1.8em;
                margin-bottom: 20px;
              }
          
              .card h3 {
                font-size: 1.5em;
                margin-bottom: 15px;
                color: var(--text-primary);
                font-weight: 600;
              }
          
              .card p {
                color: var(--text-secondary);
                line-height: 1.7;
                margin-bottom: 25px;
                font-size: 0.95em;
              }
          
              .card-link {
                color: var(--primary);
                text-decoration: none;
                font-weight: 600;
                display: inline-flex;
                align-items: center;
                gap: 8px;
                transition: gap 0.3s;
              }
          
              .card-link:hover {
                gap: 12px;
              }
          
              .card-link::after {
                content: '→';
                font-size: 1.2em;
              }
          
              /* Features Section */
              .features-section {
                margin: 80px 0;
              }
          
              .section-title {
                text-align: center;
                font-size: 2.2em;
                margin-bottom: 50px;
                color: var(--text-primary);
                font-weight: 700;
              }
          
              .features-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
                gap: 30px;
              }
          
              .feature {
                text-align: center;
                padding: 30px;
              }
          
              .feature-icon {
                width: 70px;
                height: 70px;
                margin: 0 auto 20px;
                background: var(--card-bg);
                border: 1px solid var(--border);
                border-radius: 12px;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 2em;
              }
          
              .feature h4 {
                font-size: 1.2em;
                margin-bottom: 12px;
                color: var(--text-primary);
              }
          
              .feature p {
                color: var(--text-secondary);
                font-size: 0.95em;
                line-height: 1.6;
              }
          
              /* Footer */
              .footer {
                background: var(--card-bg);
                border-top: 1px solid var(--border);
                padding: 60px 40px 40px;
                margin-top: 80px;
              }
          
              .footer-content {
                max-width: 1400px;
                margin: 0 auto;
              }
          
              .footer-grid {
                display: grid;
                grid-template-columns: 2fr 1fr 1fr;
                gap: 60px;
                margin-bottom: 50px;
              }
          
              .footer-section h4 {
                color: var(--text-primary);
                font-size: 1.1em;
                margin-bottom: 20px;
                font-weight: 600;
              }
          
              .footer-section p {
                color: var(--text-secondary);
                line-height: 1.7;
                margin-bottom: 20px;
              }
          
              .footer-links {
                display: flex;
                flex-direction: column;
                gap: 12px;
              }
          
              .footer-link {
                color: var(--text-secondary);
                text-decoration: none;
                transition: color 0.3s;
                display: flex;
                align-items: center;
                gap: 8px;
              }
          
              .footer-link:hover {
                color: var(--primary);
              }
          
              .footer-bottom {
                padding-top: 30px;
                border-top: 1px solid var(--border);
                display: flex;
                justify-content: space-between;
                align-items: center;
                flex-wrap: wrap;
                gap: 20px;
              }
          
              .org-info {
                color: var(--text-secondary);
                font-size: 0.9em;
              }
          
              .org-name {
                color: var(--primary);
                font-weight: 600;
              }
          
              .social-links {
                display: flex;
                gap: 20px;
              }
          
              .social-link {
                color: var(--text-secondary);
                text-decoration: none;
                transition: color 0.3s;
                font-size: 0.95em;
              }
          
              .social-link:hover {
                color: var(--primary);
              }
          
              @media (max-width: 1024px) {
                .footer-grid {
                  grid-template-columns: 1fr;
                  gap: 40px;
                }
              }
          
              @media (max-width: 768px) {
                .hero h1 {
                  font-size: 2.5em;
                }
          
                .hero p {
                  font-size: 1.1em;
                }
          
                .nav {
                  flex-direction: column;
                  gap: 20px;
                }
          
                .cards-grid,
                .features-grid {
                  grid-template-columns: 1fr;
                }
          
                .container {
                  padding: 0 20px 60px;
                }
          
                .footer-bottom {
                  flex-direction: column;
                  text-align: center;
                }
              }
            </style>
          </head>
          <body>
            <!-- Header -->
            <header class="header">
              <nav class="nav">
                <div class="logo-section">
                  <div class="logo">
                    <img src="https://raw.githubusercontent.com/martourez21/otter-streams/main/docs/assets/otterstream-sdk-icon.ico" alt="Otter Streams">
                  </div>
                  <span class="brand">Otter Streams</span>
                </div>
                <div class="nav-links">
                  <a href="https://github.com/martourez21/otter-streams" class="nav-link">GitHub</a>
                  <a href="https://github.com/martourez21/otter-streams/releases" class="nav-link">Releases</a>
                  <a href="mailto:nestorabiawuh@gmail.com" class="nav-link">Contact</a>
                </div>
              </nav>
          
              <div class="hero">
                <div class="version-tag">v${{ steps.get_version.outputs.VERSION }}</div>
                <h1>Production ML Inference<br>for Apache Flink</h1>
                <p>High-performance machine learning inference library for real-time streaming applications. Integrate ML models seamlessly into your data pipelines.</p>
              </div>
            </header>
          
            <!-- Main Content -->
            <main class="container">
              <!-- Primary Cards -->
              <div class="cards-grid">
                <div class="card">
                  <div class="card-icon"></div>
                  <h3>API & SDK Documentation</h3>
                  <p>Complete SDK  and API reference with detailed documentation for all modules, classes, and methods in the Otter Streams library.</p>
                  <a href="javadoc/${{ steps.get_version.outputs.VERSION }}/index.html" class="card-link">View Documentation</a>
                </div>
          
                <div class="card">
                  <div class="card-icon"></div>
                  <h3>Getting Started</h3>
                  <p>Quick start guides and examples to help you integrate ML inference into your Flink applications in minutes.</p>
                  <a href="https://github.com/martourez21/otter-streams#getting-started" class="card-link">Get Started</a>
                </div>
          
                <div class="card">
                  <div class="card-icon"></div>
                  <h3>Examples</h3>
                  <p>Real-world examples demonstrating best practices for ML inference in production streaming environments.</p>
                  <a href="https://github.com/martourez21/otter-streams/tree/main/otter-stream-examples" class="card-link">View Examples</a>
                </div>
              </div>
          
              <!-- Features Section -->
              <section class="features-section">
                <h2 class="section-title">Why Otter Streams?</h2>
                <div class="features-grid">
                  <div class="feature">
                    <div class="feature-icon"></div>
                    <h4>High Performance</h4>
                    <p>Optimized for low-latency inference with efficient resource management and parallel processing</p>
                  </div>
          
                  <div class="feature">
                    <div class="feature-icon">🔧</div>
                    <h4>Multi-Framework</h4>
                    <p>Native support for ONNX, TensorFlow, PyTorch, XGBoost, and PMML model formats</p>
                  </div>
          
                  <div class="feature">
                    <div class="feature-icon"></div>
                    <h4>Production Ready</h4>
                    <p>Built-in monitoring, caching, error handling, and fault tolerance for enterprise deployments</p>
                  </div>
          
                  <div class="feature">
                    <div class="feature-icon"></div>
                    <h4>Easy Integration</h4>
                    <p>Simple SDK  and API that integrates seamlessly with existing Flink applications and workflows</p>
                  </div>
          
                  <div class="feature">
                    <div class="feature-icon">☁</div>
                    <h4>Cloud Native</h4>
                    <p>Support for remote inference endpoints including AWS SageMaker, GCP Vertex AI, and Azure ML</p>
                  </div>
          
                  <div class="feature">
                    <div class="feature-icon"></div>
                    <h4>Scalable</h4>
                    <p>Designed to scale horizontally with your Flink cluster for high-throughput workloads</p>
                  </div>
                </div>
              </section>
            </main>
          
            <!-- Footer -->
            <footer class="footer">
              <div class="footer-content">
                <div class="footer-grid">
                  <div class="footer-section">
                    <h4>About Otter Streams</h4>
                    <p>Otter Streams is a production-grade ML inference library designed specifically for Apache Flink streaming applications. Built with performance and reliability in mind for enterprise deployments.</p>
                  </div>
          
                  <div class="footer-section">
                    <h4>Resources</h4>
                    <div class="footer-links">
                      <a href="https://github.com/martourez21/otter-streams" class="footer-link"> Repository</a>
                      <a href="https://github.com/martourez21/otter-streams/releases" class="footer-link"> Releases</a>
                      <a href="https://github.com/martourez21/otter-streams/issues" class="footer-link"> Issues</a>
                      <a href="javadoc/${{ steps.get_version.outputs.VERSION }}/index.html" class="footer-link"> SDK  and API Docs</a>
                    </div>
                  </div>
          
                  <div class="footer-section">
                    <h4>Contact</h4>
                    <div class="footer-links">
                      <a href="mailto:nestorabiawuh@gmail.com" class="footer-link">✉️ Email</a>
                      <a href="https://www.linkedin.com/in/nestor-abiangang/" class="footer-link" target="_blank">💼 LinkedIn</a>
                      <a href="https://github.com/martourez21" class="footer-link" target="_blank">👨‍💻 GitHub</a>
                    </div>
                  </div>
                </div>
          
                <div class="footer-bottom">
                  <p class="org-info">Developed by <span class="org-name">Nestor Martourez @ CodedStreams</span> © 2025</p>
                  <div class="social-links">
                    <span class="social-link">Apache License 2.0</span>
                  </div>
                </div>
              </div>
            </footer>
          </body>
          </html>
          EOF</a>
                  </div>
                </div>
          
                <div class="contact">
                  <h4>Developer Contact</h4>
                  <div class="contact-links">
                    <a href="mailto:nestorabiawuh@gmail.com" class="contact-link">
                      ✉️ nestorabiawuh@gmail.com
                    </a>
                    <a href="https://www.linkedin.com/in/nestor-abiangang/" class="contact-link" target="_blank" rel="noopener noreferrer">
                      💼 LinkedIn Profile
                    </a>
                  </div>
                  <div class="org-badge">Developed by Nestor Martourez @ CodedStreams</div>
                </div>
              </div>
            </div>
          </body>
          </html>
          EOF

      - name: Deploy index to GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./gh-pages-root
          keep_files: true
        continue-on-error: true

  publish-release:
    needs: [build, code-quality]
    runs-on: ubuntu-latest
    if: github.event_name == 'release' && github.event.action == 'published'
    permissions:
      contents: write
      packages: write
      pages: write
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 11
        uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
          cache: 'maven'

      - name: Configure Maven settings for GitHub Packages
        run: |
          mkdir -p ~/.m2
          cat > ~/.m2/settings.xml << EOF
          <settings>
            <servers>
              <server>
                <id>github</id>
                <username>\${env.GITHUB_ACTOR}</username>
                <password>\${env.GITHUB_TOKEN}</password>
              </server>
            </servers>
          </settings>
          EOF

      - name: Extract version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Update Maven version
        run: mvn versions:set -DnewVersion=${{ steps.version.outputs.VERSION }} -DgenerateBackupPoms=false

      - name: Build release artifacts with documentation
        run: mvn -B clean install javadoc:jar source:jar -DskipTests -Dmaven.javadoc.failOnError=false -Dmaven.javadoc.failOnWarnings=false

      - name: Generate aggregated Javadoc
        run: mvn -B javadoc:aggregate -Dmaven.javadoc.failOnError=false -Dmaven.javadoc.failOnWarnings=false
        continue-on-error: true

      - name: Create release package
        run: |
          mkdir -p release-artifacts
          # Copy all JAR files (main, sources, javadoc)
          find . -name "*.jar" \
            -not -path "*/target/original-*" \
            -not -path "*/target/*-tests.jar" \
            -exec cp {} release-artifacts/ \;
          
          echo "Release artifacts:"
          ls -lh release-artifacts/

      - name: Generate individual module Javadoc archives
        run: |
          for module in ml-inference-core otter-stream-onnx otter-stream-tensorflow otter-stream-pytorch otter-streams-xgboost otter-stream-pmml otter-stream-remote otter-stream-examples; do
            if [ -d "$module/target/apidocs" ]; then
              echo "Creating Javadoc archive for $module"
              cd $module/target
              tar -czf ../../release-artifacts/${module}-javadoc-${{ steps.version.outputs.VERSION }}.tar.gz apidocs/
              cd ../..
            else
              echo "Warning: No apidocs found for $module"
            fi
          done

      - name: Generate aggregated Javadoc archive
        run: |
          if [ -d "target/site/apidocs" ]; then
            cd target/site
            tar -czf ../../release-artifacts/otter-streams-javadoc-aggregated-${{ steps.version.outputs.VERSION }}.tar.gz apidocs/
          else
            echo "Warning: No aggregated apidocs found"
          fi

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: release-artifacts/*
          fail_on_unmatched_files: false
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Deploy aggregated Javadoc to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./target/site/apidocs
          destination_dir: javadoc/${{ steps.version.outputs.VERSION }}
          keep_files: true

      - name: Update Javadoc index with release version
        run: |
          mkdir -p gh-pages-root
          cat > gh-pages-root/index.html << 'EOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>Otter Streams - ML Inference for Apache Flink | SDK  and API Documentation</title>
            <meta name="description" content="Production-grade ML inference library for Apache Flink streaming applications">
            <link rel="icon" href="https://raw.githubusercontent.com/martourez21/otter-streams/main/docs/assets/otterstream-sdk-icon.ico" type="image/x-icon">
            <style>
              * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
              }
          
              body {
                font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
                line-height: 1.6;
                color: #333;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                min-height: 100vh;
                padding: 20px;
              }
          
              .container {
                max-width: 900px;
                margin: 0 auto;
                background: white;
                border-radius: 20px;
                box-shadow: 0 20px 60px rgba(0,0,0,0.3);
                overflow: hidden;
              }
          
              .header {
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                padding: 60px 40px;
                text-align: center;
              }
          
              .logo {
                width: 120px;
                height: 120px;
                margin: 0 auto 20px;
                background: white;
                border-radius: 20px;
                padding: 15px;
                box-shadow: 0 10px 30px rgba(0,0,0,0.2);
              }
          
              .logo img {
                width: 100%;
                height: 100%;
                object-fit: contain;
              }
          
              h1 {
                font-size: 2.5em;
                font-weight: 700;
                margin-bottom: 10px;
                text-shadow: 0 2px 10px rgba(0,0,0,0.2);
              }
          
              .subtitle {
                font-size: 1.2em;
                opacity: 0.95;
                font-weight: 300;
              }
          
              .content {
                padding: 40px;
              }
          
              .description {
                font-size: 1.1em;
                color: #666;
                margin-bottom: 40px;
                text-align: center;
                line-height: 1.8;
              }
          
              .version-card {
                background: linear-gradient(135deg, #00cc6615 0%, #00994d15 100%);
                border-radius: 15px;
                padding: 30px;
                margin-bottom: 30px;
                border-left: 5px solid #00cc66;
                transition: transform 0.3s ease, box-shadow 0.3s ease;
              }
          
              .version-card:hover {
                transform: translateY(-5px);
                box-shadow: 0 10px 30px rgba(0, 204, 102, 0.2);
              }
          
              .version-card h3 {
                color: #00994d;
                font-size: 1.3em;
                margin-bottom: 15px;
                display: flex;
                align-items: center;
                gap: 10px;
              }
          
              .version-badge {
                background: #00cc66;
                color: white;
                padding: 5px 15px;
                border-radius: 20px;
                font-size: 0.85em;
                font-weight: 600;
              }
          
              .release-badge {
                background: linear-gradient(135deg, #00cc66 0%, #00994d 100%);
                color: white;
                padding: 5px 15px;
                border-radius: 20px;
                font-size: 0.75em;
                font-weight: 600;
              }
          
              .version-card a {
                color: #764ba2;
                text-decoration: none;
                font-size: 1.1em;
                font-weight: 600;
                display: inline-flex;
                align-items: center;
                gap: 8px;
                transition: color 0.3s ease;
              }
          
              .version-card a:hover {
                color: #667eea;
              }
          
              .version-card a::after {
                content: '→';
                font-size: 1.2em;
                transition: transform 0.3s ease;
              }
          
              .version-card a:hover::after {
                transform: translateX(5px);
              }
          
              .features {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 20px;
                margin: 40px 0;
              }
          
              .feature {
                padding: 20px;
                background: #f8f9fa;
                border-radius: 10px;
                text-align: center;
              }
          
              .feature-icon {
                font-size: 2em;
                margin-bottom: 10px;
              }
          
              .feature h4 {
                color: #667eea;
                margin-bottom: 10px;
              }
          
              .feature p {
                color: #666;
                font-size: 0.95em;
              }
          
              .footer {
                background: #f8f9fa;
                padding: 30px 40px;
                border-top: 1px solid #e9ecef;
              }
          
              .footer-content {
                display: flex;
                justify-content: space-between;
                align-items: center;
                flex-wrap: wrap;
                gap: 20px;
              }
          
              .footer-links {
                display: flex;
                gap: 20px;
                flex-wrap: wrap;
              }
          
              .footer-link {
                color: #667eea;
                text-decoration: none;
                font-weight: 500;
                transition: color 0.3s ease;
              }
          
              .footer-link:hover {
                color: #764ba2;
              }
          
              .contact {
                margin-top: 30px;
                padding-top: 30px;
                border-top: 1px solid #e9ecef;
                text-align: center;
              }
          
              .contact h4 {
                color: #333;
                margin-bottom: 15px;
              }
          
              .contact-links {
                display: flex;
                justify-content: center;
                gap: 25px;
                flex-wrap: wrap;
              }
          
              .contact-link {
                display: inline-flex;
                align-items: center;
                gap: 8px;
                color: #667eea;
                text-decoration: none;
                font-weight: 500;
                transition: color 0.3s ease;
              }
          
              .contact-link:hover {
                color: #764ba2;
              }
          
              .org-badge {
                display: inline-block;
                background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
                color: white;
                padding: 8px 20px;
                border-radius: 20px;
                font-weight: 600;
                margin-top: 10px;
              }
          
              @media (max-width: 768px) {
                h1 {
                  font-size: 2em;
                }
          
                .header {
                  padding: 40px 20px;
                }
          
                .content {
                  padding: 30px 20px;
                }
          
                .footer-content {
                  flex-direction: column;
                  text-align: center;
                }
          
                .features {
                  grid-template-columns: 1fr;
                }
              }
            </style>
          </head>
          <body>
            <div class="container">
              <div class="header">
                <div class="logo">
                  <img src="https://raw.githubusercontent.com/martourez21/otter-streams/main/docs/assets/otterstream-sdk-icon.ico" alt="Otter Streams Logo">
                </div>
                <h1>Otter Streams</h1>
                <p class="subtitle">ML Inference for Apache Flink</p>
              </div>
          
              <div class="content">
                <p class="description">
                  Production-grade ML inference library for Apache Flink streaming applications. 
                  Seamlessly integrate machine learning models into your real-time data pipelines.
                </p>
          
                <div class="version-card">
                  <h3>
                    Latest Stable Release
                    <span class="version-badge">${{ steps.version.outputs.VERSION }}</span>
                    <span class="release-badge">STABLE</span>
                  </h3>
                  <a href="javadoc/${{ steps.version.outputs.VERSION }}/index.html">
                    View SDK  and API Documentation
                  </a>
                </div>
          
                <div class="features">
                  <div class="feature">
                    <div class="feature-icon">🚀</div>
                    <h4>High Performance</h4>
                    <p>Optimized for low-latency ML inference in streaming applications</p>
                  </div>
                  <div class="feature">
                    <div class="feature-icon">🔧</div>
                    <h4>Multi-Framework</h4>
                    <p>Support for ONNX, TensorFlow, PyTorch, XGBoost, and PMML</p>
                  </div>
                  <div class="feature">
                    <div class="feature-icon">📊</div>
                    <h4>Production Ready</h4>
                    <p>Built-in monitoring, caching, and error handling</p>
                  </div>
                </div>
              </div>
          
              <div class="footer">
                <div class="footer-content">
                  <div class="footer-links">
                    <a href="https://github.com/martourez21/otter-streams" class="footer-link">📦 GitHub Repository</a>
                    <a href="https://github.com/martourez21/otter-streams/releases" class="footer-link">📋 Releases</a>
                    <a href="https://github.com/martourez21/otter-streams/issues" class="footer-link">🐛 Issues</a>
                  </div>
                </div>
          
                <div class="contact">
                  <h4>Developer Contact</h4>
                  <div class="contact-links">
                    <a href="mailto:nestorabiawuh@gmail.com" class="contact-link">
                      ✉️ nestorabiawuh@gmail.com
                    </a>
                    <a href="https://www.linkedin.com/in/nestor-abiangang-a" class="contact-link" target="_blank" rel="noopener noreferrer">
                      💼 LinkedIn Profile
                    </a>
                  </div>
                  <div class="org-badge">Built by CodedStreams</div>
                </div>
              </div>
            </div>
          </body>
          </html>
          EOF

      - name: Deploy updated index to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./gh-pages-root
          keep_files: true

      - name: Publish to GitHub Packages
        run: mvn -B deploy -DskipTests
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  notify:
    needs: [build, code-quality, package]
    runs-on: ubuntu-latest
    if: always()

    steps:
      - name: Check build status
        run: |
          if [ "${{ needs.build.result }}" == "failure" ] || [ "${{ needs.code-quality.result }}" == "failure" ]; then
            echo "Build or quality checks failed"
            exit 1
          else
            echo "All checks passed"
          fi